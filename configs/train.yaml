hydra:
  run:
    dir: 'trained_models/${now:%Y-%m-%d-%H-%M-%S}'
  output_subdir: '.'

defaults:
  - _self_
  - data: clinical
  - model: baseline
  - callbacks: default
  - trainer: SLURM
  - logger: wandb

train: True

record_memory: False